[package]
name = "cuda-neural-compiler"
version = "0.2.0"
edition = "2021"
description = "Weight quantization and METL binary — uses cuda-equipment"

[dependencies]
# cuda-equipment = { path = "../cuda-equipment" }
